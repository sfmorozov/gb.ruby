2. SELECT
    id,
    flow_number,
    started_at
    FROM streams
    ORDER BY started_at DESC
    LIMIT 1;

5. SELECT
    id,
    started_at
    FROM streams
    ORDER BY started_at DESC
    LIMIT 2;